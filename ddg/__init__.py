"""Document Dependency Graphs - an evidence-bearing audit workbench.

The graph is an index over immutable source snapshots. It never replaces the
files, never modifies them, and never executes anything a reviewer has not
approved.
"""

__version__ = "0.1.0"
PARSER_VERSION = "ddg-parsers/0.1.0"
CHECKER_VERSION = "ddg-checkers/0.1.0"
